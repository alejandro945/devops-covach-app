apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "next-app.fullname" . }}-test-connection"
  labels:
    {{- include "next-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "next-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
